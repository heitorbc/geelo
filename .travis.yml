dist: trusty
sudo: false
language: python
python:
  - "3.4"

branches:
  only: 
    - master

install:
  - pip install -r requirements.txt

script: nosetests

deploy:
 provider: heroku
 api-key: 
  secure: $HEROKU_API_KEY

notifications:
    slack: geelo:9B7fJH0r7NfGbGOlCMUcFtwx

